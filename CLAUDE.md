@AGENTS.md

# Portfolio — Claude Code Context

## Що це за проєкт
Premium портфоліо-сайт для Serhii Tarasiuk (AI Product Builder & Creative Technologist, Kyiv). Окремий репозиторій від `Foody` (`c:/Users/WellDone/projects/portfolio`). Editorial-дизайн з 3D hero-сценою (React Three Fiber), три "світи" (3D / Video / Developers), білінгвальний (en/uk), світла/темна тема.

Побудований за `docs/brief.md` (первинний майстер-спек) — але **актуальний стан дизайну документований у `docs/design-decisions.md`**, не в brief.md. Brief.md — історичний вхідний документ, design-decisions.md — живий журнал усіх наступних правок (20+ "pivot"-записів), включно з тим, де рішення явно замінюють/скасовують пункти з brief.md чи попередніх pivot-ів.

## Технологічний стек
- **Framework**: Next.js 16.2.10 (App Router, Turbopack, `proxy.ts` замість `middleware.ts`)
- **Language**: TypeScript strict
- **3D**: React Three Fiber (`@react-three/fiber`) + `@react-three/drei` — hero canvas (`HeroOrbitScene`, avatar, per-world backdrops, grid-scroll motion)
- **Styling**: Tailwind CSS v4
- **Theming**: власний light/dark toggle (`ThemeContext`, cookie-persisted, server-read у root layout щоб уникнути flash) — **scoped навмисно**: тільки chrome/контентні сторінки (header, footer, About/Work/Contact, картки, кейс-стаді). 3D hero canvas і per-world палітра (`worldTheme.ts`) НЕ реагують на toggle — це свідома атмосферна темна вітрина незалежно від теми
- **Animation**: `motion` (Framer Motion для React 19), плюс CSS transform/keyframe трюки (bio card thickness, drift, stagger fade, typewriter)
- **i18n**: власна легка система (не next-intl) — `[locale]` route segment (`en`/`uk`), `src/content/copy.en.ts`/`copy.uk.ts`, типобезпечно звірені одне з одним через `src/content/copy.ts` (`CopyDict = typeof en`)
- **Тести**: Playwright (`tests/*.spec.ts`) — navigation, accessibility, responsive (no-console-errors, no horizontal overflow), projects/world-carousel
- **Контент**: без CMS/БД — усе в `src/content/*.ts` (profile, projects, worldTheme, copy, videos, toolLogos, figurines)
- **Медіа**: self-hosted відео на Hetzner через Coolify+Caddy (`NEXT_PUBLIC_MEDIA_BASE_URL`), деталі — [docs/media-hosting.md](docs/media-hosting.md). Медіа-контейнер (`media-server`, `caddy:2-alpine`) — **не Coolify-ресурс**, підняли вручну через Docker з Traefik-лейблами, тому не видний у Coolify Projects, лише в його container-level Terminal. Bind mount: `/data/media` (хост) → `/srv/media` (контейнер, ro). Відео-файли зараз оптимізовані (ffmpeg, H.264/AAC + `+faststart`, лежать з poster-jpg поруч) — сирі оригінали збережені в `assets-source/video-creator-originals/`, не видалені.
- **AI-чат**: плаваючий "ask about Serhii" віджет (`src/components/ai-agent/ChatWidget.tsx`), Claude API напряму через `fetch` (без SDK-залежності) з `src/app/api/chat/route.ts`, ключ — `ANTHROPIC_API_KEY` (server-only). Системний промпт (`src/lib/aiAgentContext.ts`, `buildAiAgentSystemPrompt()`) збирається з реального контенту (`profile.ts`/`projects.ts`/`copy.en.ts`), не хардкоджений — це навмисно, щоб не розходився з реальним сайтом. Відповідь агента дозволяє `**bold**` (рендериться як справжній `<strong>` через `renderFormattedText()` у `ChatWidget.tsx`, будь-які "осиротілі" зірочки прибираються) і доречні смайли — інших markdown-конструкцій нема. Мобільний input — `text-base` (16px), не менше: iOS Safari авто-зумить сторінку на фокус будь-якого інпута під 16px, це і була причина "сторінку треба стискати назад" багу; крім того `overflow: hidden` на `document.body`, поки чат відкритий, і `[locale]/layout.tsx`'s `viewport` export (`interactiveWidget: "resizes-content"`) — без цього мобільна клавіатура ламала fixed-позиціонування панелі.
- **Голосовий дзвінок**: яскрава зелена banner-кнопка (`VoiceCallButton.tsx`, `bg-green-500` + `.animate-cta-bounce` — той самий bounce-keyframe що й "Let's connect" CTA) одразу під заголовком чат-панелі (не маленька іконка збоку — навмисно гучна, щоб привертати увагу) відкриває `VoiceCallModal` — жива розмова через OpenAI Realtime API по WebRTC (Claude не має еквівалентного speech-to-speech API, тож для цієї конкретної модальності — інший вендор; текстовий чат лишається на Claude). Grounding — та сама `buildAiAgentSystemPrompt()`, обгорнута в `buildVoiceAgentInstructions()` (`aiAgentContext.ts`) з голосовими правилами зверху (коротко, без markdown, привітання спершу) — свідомо не окремий retrieval tool/endpoint, як пропонував початковий ChatGPT-спек: весь grounding уже вміщається в один system-prompt рядок, тож retrieval не потрібен. Перед Turnstile — обов'язковий email-крок (`EmailStep` у `VoiceCallModal.tsx`, валідація — спільна `isValidEmail()` з `src/lib/voice/email.ts`, використана і клієнтом, і сервером; форма — `noValidate`, бо нативна HTML5-валідація `type="email"` інколи перехоплює submit ДО власного onSubmit-хендлера і власне локалізоване повідомлення про помилку просто не встигає показатись): email летить у `/api/voice/session`, який одразу після проходження Turnstile+rate-limit шле "дзвінок почався"-нотіс у Telegram з цим email — це гарантований сигнал Сергію про кожен реальний дзвінок (не лише коли агент чогось не знає), і саме тому email обов'язковий, а не опційний. Той самий email тягнеться і в `save_question_for_serhii` → `/api/voice/unanswered`, щоб було видно, з якого емейлу прийшло конкретне питання. Єдиний function tool — `save_question_for_serhii` (`src/lib/voice/tools.ts`), результат летить у Telegram Сергія (`src/lib/voice/telegram.ts`, `sendTelegramMessage()`) без жодної БД (Supabase в цьому репо нема — воно лише у Foody). `POST /api/voice/session` (`src/app/api/voice/session/route.ts`) звіряє Cloudflare Turnstile-токен (`src/lib/voice/turnstile.ts`) і per-IP rate limit (`src/lib/voice/rateLimit.ts`, in-memory `Map` — безпечно саме тому, що сайт крутиться як один Node-процес на одному Coolify/Hetzner-контейнері, не serverless) перед тим як мінтити ephemeral `client_secret` через `POST https://api.openai.com/v1/realtime/client_secrets`; сам real `OPENAI_API_KEY` ніколи не йде в браузер. Клієнтський WebRTC-хендшейк (`src/lib/voice/webrtcClient.ts`) шле SDP offer напряму на `https://api.openai.com/v1/realtime/calls` з ephemeral-ключем — це вже безпечно робити з браузера. У модалі — аватар (`BIO_CARD_PORTRAIT_SRC`, той самий портрет що й bio-куб на головній, той самий server-side `publicAssetExists`-патерн, `hasPortrait` тягнеться з root layout через `ChatWidget` пропом; фолбек — гліф-емодзі, якщо файлу нема) всередині `VoiceVisualizer` — той тепер малює не бар-еквалайзер, а пульсуючий glow-ring навколо аватара (react на audio-level ref), видний протягом усього життя вікна дзвінка, не лише в момент реального конекту. Жорсткий ліміт розмови — 2 хвилини (`CALL_DURATION_S` у `VoiceCallModal.tsx`, сам рахує elapsed time, не покладається на бекенд): за 10с до кінця — "wrapping up"-нотіс в UI, за 10с до кінця — `response.cancel` якщо агент саме говорить, на нулі — повний teardown (peer connection, data channel, mic tracks, audio playback) незалежно від того, що відбувається. Той самий teardown (`endCall`) також спрацьовує на hang-up/Escape/X/помилку/unmount — і **навмисно відкладений на один тік через `setTimeout(...,0)`** у mount/unmount-ефекті: React Strict Mode в деві синхронно робить mount→cleanup→mount при першому монтуванні (щоб ловити саме такі неідемпотентні cleanup-баги), і без цієї відстрочки цей самий ефект передчасно завершував дзвінок ще до того, як користувач встиг щось зробити — спіймано реальним runtime-тестом (Playwright), не здогадкою. Turnstile-віджет (`TurnstileWidget.tsx`) — ванільний `challenges.cloudflare.com/turnstile/v0/api.js` через `next/script`, без нового npm-пакета (та сама логіка, що й пропуск Anthropic SDK для текстового чату). Змінні середовища — `OPENAI_API_KEY`/`OPENAI_REALTIME_MODEL`, `NEXT_PUBLIC_TURNSTILE_SITE_KEY`/`TURNSTILE_SECRET_KEY`, `TELEGRAM_BOT_TOKEN`/`TELEGRAM_OWNER_CHAT_ID` — усі задокументовані в `.env.example`, без жодної з них фіча деградує гейсфулли (кнопка рендериться, показує "not configured"), не крашиться.

## Структура проєкту
```
src/
  app/[locale]/        # en/uk маршрути; / редіректить через src/proxy.ts
    layout.tsx           # root layout: читає theme-cookie server-side, монтує ThemeProvider/WorldNavProvider/GridScrollProvider + AmbientBackground (глобально, один раз)
    page.tsx             # головна — 3D hero (HeroOrbitScene) + секції нижче
    about/                # About: WorldTitleCube (ім'я, той самий glass-cube що й на /work), AboutVideoPanel (справжній 9:16 box), typewriter-опис
    work/                 # /work — WorldChooserBlocks (3 world-блоки + 2 extra-блоки Game Dev/AI Creator, fly-to-center transition тільки для world-блоків) + SelectedWorkList
      [slug]/               # кейс-стаді конкретного проєкту — hero (BackToWorkButton+LogoBadge+WorldTitleCube+CTA-лінк) + StaggerFadeIn-картки (Role/Stack/Summary/Challenge/Diagram/Screenshots/Video/Approach/VerificationNote), кожен тип картки — своя унікальна форма (clip-path кути, notch-заголовок на Stack, viewfinder-кути на Video тощо), root — overflow-x-hidden (гейтить transient overflow від slide-* StaggerFadeIn variants на вузьких viewport). Chip/card padding на мобільному компактніший за md:-версію (не лише один розмір). "Screenshots"-блок (`ProductImageSlider`) рендериться лише коли `project.media` має `kind: "screenshot"` записи — на 2026-07-13 жоден проєкт їх не має (ships empty, як figurines.ts)
      3d/ video/ developers/ games/ ai-creator/  # gallery pages — ЗАРЕЗЕРВОВАНІ слаги, ніколи не використовувати як project.slug (games/ai-creator НЕ ProjectWorld, лише /work-сторінкові блоки, без 3D-прив'язки)
      3d/metaverse/           # "Метавсесвіт" — світла кімната (R3F, OrbitControls-фляй-камера), 6 фіксованих п'єдесталів; порожні до появи записів у figurines.ts, тоді кожен показує відео-петлю через drei Html. Вхід — кнопка "3D View" на amorphous-порталі кейс-стаді 3d-lab
      video/[category]/      # окрема сторінка з повним списком кліпів однієї категорії (lightbox тут; на /work/video прев'ю лише autoPlay, не відкриваються)
    contact/              # окрема сторінка існує, header Connect веде до dropdown, не сюди напряму
  components/
    hero/                 # HeroOrbitScene (canvas), GridScrollControl (toggle+slider, за замовчуванням on/max), ConnectBurstButton, ProjectSliderPanel (drum-карусель — лише реальні проєкти, без ghost-заглушок; клік на картку фрізить обертання і ВИБИРАЄ її на місці — accent-outline glow + невеликий локальний translateZ-pop, "View case study"/"Back" pill-пара з'являється під барабаном, плаский 2D-оверлей поза 3D groupRef-деревом; НЕ fly-to-center/blackout, той ефект прибрано; праворуч SpinButton — впорскує велику одноразову velocity в ту саму flick-decay фізику, що й ручний драг), DrumProjectCard (барабанний silhouette — eliptичний border-radius через inline style, бо GlassPanel's rounded-2xl клас плоским cn() не перебивається; BarrelShading — суто декоративний cylindrical-shading оверлей; `isSelected` prop керує glow+pop)
    scene/                 # 3D-примітиви: Avatar, BioCard (~4 клікабельні грані — клік (не драг) на поточну грань відкриває BioCardDetailModal через createPortal у document.body), BioCardDetailModal, CameraRig, WorldBackdrop
      metaverse/              # MetaverseCanvasGate (WebGL/reduced-motion гейт, той самий патерн що й HeroSceneGate), MetaverseRoom (стіни/підлога/світло, фіксовані 6 SLOTS), Pedestal (плінт + опційна drei Html відео-петля з figurines.ts, той самий distanceFactor-патерн що й BioCard)
      backdrops/              # per-world "сигнатурний рух", вмикається grid-scroll кнопкою:
                               #   ServerColumnField (Developers) — вертикальні лінії-лампи, fade-колір
                               #   PrintRingField (3D) — RingVase (3 штуки: одна що "друкується" + 2 нових справа), еквалайзер-пульс
                               #   FrameCardField (Video) — FallingDonuts (обертання + нескінченне падіння)
                               #   FloorGrid — спільна сітка-підлога всіх трьох світів, скролиться при увімкненому контролі
                               #   FloatingDebris — генерик debris (box/octahedron), torus замінено на FallingDonuts у Video
    layout/                # SiteHeader (Server Component, мобільний layout — ОДИН ряд: home-іконка + WorldSwitchHeaderNav (без текст-лейбла на мобільному, лише стрілки) + HeroGridScrollBar (у власному `flex-1 min-w-0` слоті — росте лише в залишок простору, ніколи не зсуває наступні кнопки) + ThemeToggle + MobileMenu) + client-острівці: WorldSwitchHeaderNav, HeroGridScrollBar, HeaderConnectButton, ThemeToggle (dual-render: desktop nav row і мобільний хедер-рядок одразу біля hamburger-кнопки, видний без відкриття меню), LanguageSwitch (справжні SVG-прапори через FlagIcon, НЕ emoji; на мобільному тепер живе всередині MobileMenu-дропдауна, у ряд з Work/About/Connect), HeaderLogoButton (dual-render: текст-пілюля `md:`+, кругла home-іконка на мобільному), MobileMenu (Work/About — прямі лінки, Connect — один toggle що розкриває Telegram/Email/Twitter рядком (flex-row, не стовпчик), той самий патерн що й HeaderConnectButton; LanguageSwitch — праворуч у тому ж flex-ряду; ThemeToggle більше не тут, переїхав у SiteHeader), HeaderHeightVar
    sections/               # homepage секції нижче hero: ToolLogosMarquee (2-рядковий лого-marquee одразу після hero, картки — фіксовано світлі (off-white) незалежно від теми, бо частина tool-логотипів темна-на-прозорому і губилась на темному surface), Capabilities, Workflow, AgentNative — усі StaggerFadeIn-картки/timeline-стиль
    work/                  # WorldChooserBlocks, ExtraWorkBlockLink (Game Dev/AI Creator, plain Link без transition), WorldGallery (root — overflow-x-hidden safety net проти мобільного горизонтального скролу), ExtraWorkGalleryShell (спільний header-shell для Games/AI Creator/video-category сторінок), GlassProjectBlock (лінк на кейс-стаді несе `?from=<world>` — контекст для BackToWorkButton), LogoBadge (лого/glyph-бейдж біля назви), BackToWorkButton (опційний backHref для вкладених сторінок), DevelopersFilter (Apps/Websites/Games таби), VideosByCategory, VideoCategoryGrid, AiCreatorVideoGrid, FigurineCard/FigurineDetailModal/FigurineGallery (3D-друк, ships empty), MetaversePortalBlock (amorphous iridescent-blob портал на кейс-стаді 3d-lab — bare ProcessLoop + "3D View"-лінк на /work/3d/metaverse), ProjectCardFlagship/Compact, WorldTitleCube (theme-aware — світла тема: темний текст + блідіша glass-панель + softened glow; темна тема — без змін), ProductImageSlider (правий-у-лівий marquee скріншотів/логотипів продукту — той самий REPEAT+`.animate-marquee-left` патерн що й ToolLogosMarquee, клік відкриває ImageLightbox; рендериться лише коли `project.media` має `kind: "screenshot"`, ships empty)
    about/                  # AboutVideoPanel
    graphics/               # кастомні SVG/CSS-діаграми для кейс-стаді (кожна — своя "shape grammar", не переюзана): FoodyFlowDiagram, PipelineDiagram, ProcessLoop (опційний `bare` — без бордер/фон-обгортки, для вбудовування в MetaversePortalBlock), AgentLoopDiagram (тепер не статична діаграма — нескінченна horizontal-стрічка, драг + wheel-scroll, wrap через `position mod lapWidth`, той самий rAF-refs-патерн що й drum-карусель)
    ai-agent/               # ChatWidget — плаваючий чат-віджет, глобально в root layout поруч з SiteFooter (не тільки на homepage); хедер панелі має VoiceCallButton (телефон-іконка) поруч з заголовком
    voice/                  # VoiceCallModal (весь call lifecycle: Turnstile → session → WebRTC → 60с ліміт → teardown), VoiceCallButton (тригер), VoiceVisualizer (bar-метр, ref-driven rAF, не React state), CallTimer (чистий display, авторитетний countdown живе в модалі), TurnstileWidget (ванільний Cloudflare-скрипт через next/script, без нового npm-пакета)
    ui/                     # GlassPanel (спільний glass-рецепт, theme-aware через --glass-* CSS vars), StaggerFadeIn (опційний `variant`: rise/slide-left/slide-right/scale, дефолт rise = старий behavior; mounted-стан резолвиться в `transform: "none"`, НЕ у щось на кшталт translateY(0) — див. gotcha нижче), TypewriterText, CopyEmailButton, AmbientBackground (глобальний, монтується раз у layout), PageTitleWatermark (theme-aware textShadow — вимкнений у світлій темі, див. gotcha нижче), VideoCard (uniform-aspect thumbnail, `onOpen` опційний — без нього рендериться неклікабельний `<div>`, не `<button>`; autoPlay-картки тепер `useInView`-гейтовані — не грають/не вантажаться, поки картка не близько до viewport; для `video-creator/`-кліпів — `poster` + `preload="none"` замість `"metadata"`, скоуплено префіксом шляху щоб не 404-ити на кейс-стаді відео без poster), VideoLightbox (fullscreen custom player, той самий poster-скоуп), ImageLightbox (той самий shell що й VideoLightbox, без playback-контролів — для ProductImageSlider)
  content/                # profile.ts, projects.ts (PortfolioProject[], developerCategory?), worldTheme.ts (+extraWorkAccents для Games/AI Creator, +signalTextVar на кожен WorldTheme — var()-рядок на light-theme-safe текстовий токен, НЕ сам signal), copy.en/uk.ts, copy.ts (CopyDict), assetPaths.ts, videos.ts (VideoItem[], VIDEO_CATEGORIES — джерело істини для категорій), toolLogos.ts (лого-marquee), figurines.ts (Figurine[], ships empty — 3D-друк, до реальних відео-петель; тепер два споживачі — FigurineGallery (/work/3d) і Pedestal (/work/3d/metaverse), обидва рендерять нуль елементів, поки масив порожній), types.ts (ThemeMode тут теж)
  context/                # WorldNavContext (активний світ), GridScrollContext (раніше SpotlightContext — той самий toggle+slider UI, тепер керує per-world рухом замість прожектора; за замовчуванням `on: true`, `speed` на максимумі), ThemeContext (light/dark, cookie-synced)
  lib/                    # cn.ts, seo.ts, media.ts, publicAsset.ts (server-side fs.existsSync для опційних асетів), locale-cookie.ts, theme-cookie.ts, aiAgentContext.ts (buildAiAgentSystemPrompt() — текстовий системний промпт з profile/projects/copy.en; buildVoiceAgentInstructions() — те саме + голосові правила зверху, не окремий hand-written промпт)
    voice/                  # tools.ts (SAVE_QUESTION_TOOL — єдиний function tool голосового агента), rateLimit.ts (in-memory per-IP Map, безпечно лише тому що один Node-процес на одному контейнері), turnstile.ts (verifyTurnstileToken), telegram.ts (sendTelegramMessage, без БД), webrtcClient.ts (startVoiceCall — весь клієнтський WebRTC/data-channel протокол, включно з function-call handling)
  hooks/                  # useLerpedColor, useMinWidth, useWebglSupport (useMotionCapability → {ready, webglSupported, reducedMotion}, useReducedMotion сам НЕ експортований), useInView (IntersectionObserver-обгортка — гейтує autoplay-відео на VideoCard), useAudioLevel (MediaStream loudness у ref, rAF-оновлюваний — для VoiceVisualizer)
  app/icon.tsx, app/apple-icon.tsx  # фавікон — next/og ImageResponse, "ST"-монограма на signal-gradient; старий статичний favicon.ico лишається як legacy fallback
  app/api/chat/route.ts    # серверний проксі до Claude API для ChatWidget — ключ (ANTHROPIC_API_KEY) лишається на сервері, zod-валідація вхідних messages
  app/api/voice/session/route.ts     # мінтить ephemeral OpenAI Realtime client_secret (Turnstile + rate limit гейтять), реальний OPENAI_API_KEY не покидає сервер
  app/api/voice/unanswered/route.ts  # save_question_for_serhii → сюди → Telegram, без БД
docs/
  brief.md                # історичний master-спек (вхідний бриф, НЕ поточний стан)
  design-decisions.md      # ЖИВИЙ журнал усіх дизайн-рішень і "pivot"-раундів — читати ПЕРШИМ для "чому так зроблено"
  content-evidence.md      # що repo-verified vs user-asserted vs потребує реальних асетів (screenshots/відео)
  media-hosting.md         # self-hosted відео на Hetzner/Coolify/Caddy
```

**Видалені за непотрібністю** (нуль референсів на момент видалення — не шукати їх, вони не існують): `AbstractFigure.tsx` (wireframe-заглушка на час завантаження аватара — прибрана, `Suspense fallback={null}`), `OrbitProjectCard.tsx`/`OrbitRing.tsx` (замінені на `DrumProjectCard`/drum-карусель), `SpotlightRig.tsx`/`SpotlightControl.tsx`/`HeroSpotlightBar.tsx`/`SpotlightContext.tsx` (весь "прожектор" замінений на `GridScrollContext`/`GridScrollControl`/`HeroGridScrollBar`), `FloatingBackgroundShapes.tsx` (замінена на глобальний `AmbientBackground`), `AnimatedName.tsx` (About-сторінка тепер використовує `WorldTitleCube`), `ToolsUsed.tsx`+`src/content/tools.ts` (текстовий "Tools I use" блок прибраний з Video Creator gallery — замінений на per-category glass-блоки з `VideosByCategory`), `AcrossLayers.tsx`/`AboutSection.tsx`/`ContactSection.tsx` (homepage-секції прибрані повністю — `/about` і `/contact` як окремі сторінки лишились).

## Ключові принципи й gotcha's
- **`docs/design-decisions.md` — джерело істини для "чому так", не код і не brief.md.** Кожен великий раунд фідбеку користувача документується як датований "pivot"-запис (тема, що змінилось, чому, які баги знайдено/виправлено). Перед нетривіальною правкою дизайну — прочитай останні pivot-записи, щоб не суперечити вже прийнятим рішенням.
- **Provider-патерн для стану, спільного між хедером і hero-сценою**: `WorldNavContext` / `GridScrollContext` / `ThemeContext` — усі однаковий легкий `useState`+`useMemo` provider, throwing `useX()` хук. Хедер (Server Component `SiteHeader`) живе в root layout вище homepage-дерева. Якщо додаєш ще один спільний стан — повторюй цей самий патерн.
- **`ThemeContext` конкретно**: initial value читається server-side з cookie в `[locale]/layout.tsx` (`next/headers` `cookies()`) і ставиться як `data-theme` на `<html>` ДО гідратації — щоб не було flash неправильної теми (той самий підхід, що й у `locale`-кукі). Світла палітра — `:root[data-theme="light"]` override блок у `globals.css`, поверх дефолтного темного `:root`. Signal/paper токени НЕ змінюються між темами — це fixed-purpose accent, не base-mode токен.
- **`--glass-tint`/`--glass-border`/`--glass-specular` CSS-змінні** — theme-aware заміна хардкоджених `bg-white/[0.06]`-style Tailwind arbitrary values у `GlassPanel` та подібних glass-поверхнях. Використовуй `bg-[var(--glass-tint)]` замість `bg-white/[0.06]` у будь-якому НОВОМУ glass-елементі поза 3D-канвасом (всередині Canvas/BioCard — навпаки, залишай завжди-темний `white/` хардкод, бо hero навмисно не реагує на тему).
- **drei's `Html` `distanceFactor` — контрінтуїтивний напрям**: БІЛЬШЕ число = БІЛЬШИЙ розмір на екрані (не менший, як здається з назви). Якщо збільшуєш CSS-ширину Html-контенту і одночасно піднімаєш `distanceFactor` "для компенсації" — це помножується, а не компенсується, і результат вибухає розміром. Якщо треба зробити елемент N разів більшим на екрані — міняй ЛИШЕ ширину, `distanceFactor` залишай як є.
- **Не вгадувати pixel-бюджети для layout-колізій — вимірювати.** Header-колізії (наприклад, `grid-cols-[1fr_auto_1fr]` з `auto`-колонкою, що росте і виштовхує сусідні `1fr`-колонки, чий вміст не переноситься) траплялись двічі за одну сесію через здогадки. Правильний підхід: запустити throwaway Playwright-скрипт, зробити `getBoundingClientRect()` реальних клікабельних елементів (не просто grid-колонок — їхній bounding box може НЕ включати overflow дочірніх елементів), і рахувати clearance у пікселях перед тим, як вважати фікс завершеним.
- **`min-w-0` на grid/flex-item дозволяє контейнеру бути вужчим за min-content дочірніх елементів — але сам вміст все одно НЕ стискається** (немає `flex-wrap`, немає індивідуального `flex-shrink` на пігулках-кнопках), тож він просто вилазить за межі свого (тепер вужчого) контейнера. Це міняє失failure mode з "вся сторінка отримує горизонтальний скролл" на "вміст візуально накладається на сусідню grid-колонку і блокує кліки" — обидва реальні баги, `min-w-0` сам по собі не "фіксить" тісний layout, лише переміщує симптом.
- **React Compiler purity-правило (`react-hooks/purity`)**: `Math.random()` заборонено в тілі рендеру, включно з ініціалізатором `useRef(...)` (виконується під час рендеру, хоч і один раз). Рандомізовані per-instance значення, які інакше пішли б у `useRef` init, — або константа на module-scope (обчислена раз при імпорті), або справжній рандом всередині `useFrame`/`useEffect` callback (це вже НЕ рендер-фаза, там `Math.random()` ок).
- **Флаг-емодзі (🇬🇧/🇺🇦) на Windows рендеряться як буквальний текст "GB"/"UA"**, не як прапор — Windows історично не постачає color-emoji шрифт, що резолвить regional-indicator-послідовності у прапор-гліфи. Реальні надійні прапори — inline SVG (`FlagIcon.tsx`), не emoji-рядок. Якщо компонент з SVG-іконкою рендериться двічі одночасно в DOM (напр. desktop+mobile рядки хедера, один прихований CSS) — `clipPath`/інші SVG `id`-атрибути мають бути унікальні через `useId()`, інакше дублікат id ламає clip у одній з копій.
- **Next.js client/server boundary pitfall**: якщо експортувати plain-константу (напр. шлях до асету, чи тип типу `ThemeMode`) з `"use client"`-файлу і імпортувати її в Server Component — Next.js підмінює **весь** експорт файлу на proxy/function, і буде `TypeError: The "path" argument must be of type string. Received function`. Спільні plain-data константи й типи (шляхи асетів, `ThemeMode` тощо) тримати в модулях БЕЗ `"use client"` (див. `src/content/assetPaths.ts`, `src/content/types.ts`).
- **Перевірка існування опційних асетів (`hasPortrait`, `hasVideoDark`/`hasVideoLight` тощо) — лише server-side** через `publicAssetExists()` (`src/lib/publicAsset.ts`, `fs.existsSync`), НЕ client-side `fetch(HEAD)`. HEAD-запит до відсутнього файлу все одно логує "Failed to load resource: 404" в консоль (Chrome логує будь-який неуспішний network request незалежно від API) — і `no console errors`-тест (`tests/responsive.spec.ts`) це ловить.
- **Слаги `3d`/`video`/`developers`/`games`/`ai-creator` — зарезервовані** (static routes `/work/{3d,video,developers,games,ai-creator}` поруч з dynamic `/work/[slug]`). Ніколи не використовувати як `project.slug` — буде permanently shadowed. `games`/`ai-creator` — НЕ `ProjectWorld` (не мають 3D backdrop/camera station/avatar rotation math), лише легкі `/work`-сторінкові блоки через `ExtraWorkBlockLink`+`ExtraWorkGalleryShell`; окремо існує Developer-world'ів фільтр-таб `developerCategory: "game"` — та сама назва "ігри", різні місця, свідомо не об'єднані.
- **Негативний margin, що витягує елемент вище за padding-бюджет предка з `overflow-hidden` — обрізається**, навіть якщо сам елемент виглядає "просто трохи піднятим у коді". Реальний баг: `AboutVideoPanel`'s `lg:-mt-24` (−96px) перевищував сторінки `lg:pt-20` (80px) top-padding, тож верх відео-панелі вилазив за межі батьківського `overflow-hidden` і обрізався — видно лише в реальному скріншоті, не в діффі. Перед тим як компенсувати layout негативним margin — рахувати точний бюджет (padding предка), не просто "трохи підняти".
- **Зміна шрифту/ваги (`font-mono` → `font-display font-bold`) на елементі в тісному header-ряду може зламати layout навіть без зміни padding** — bold sans зазвичай ширший за той самий текст моно-шрифтом. Реальний випадок: заміна шрифту Work/About/Connect-кнопок сама по собі (без зміни `px`) з'їла весь наявний clearance до center-нав на 1280px. Перевіряти `getBoundingClientRect()`-кліренс ПІСЛЯ будь-якої зміни font-family/font-weight у тісному ряду, не лише після зміни padding/розміру.
- **Кастомний child-компонент, обгорнутий у `React.forwardRef`-подібний callback-ref масив** (`useRef<(Mesh|null)[]>([])` з `ref={(el) => { refs.current[i] = el; }}`) — стандартний патерн цього проєкту для мутації N однотипних 3D/DOM-елементів щокадрово без React-рестейту (drum-карусель, lamp-fade лінії, equalizer-вази, falling donuts). Використовуй його, не `parent.children`, коли треба ітерувати динамічний список мешів/дів кожен кадр.
- **Vertical "drum"/reel-ефекти в DOM (не R3F)** — CSS 3D через `rotateX(angle) translateZ(radius)` на кожній картці (позиція фіксована, не залежить від поточного обертання) всередині батьківської групи, яка сама обертається — а не перерахунок transform кожної картки щокадру. `overflow: hidden` на **найближчому** до 3D-перспективи контейнері, не на далекому предку — 3D-трансформовані елементи можуть "втекти" за межі overflow:hidden предка, якщо він недостатньо близько в дереві (спостережуваний браузерний квірк, не лише теоретичний).
- **Доступність animated show/hide dropdown-контенту** (header Connect, mobile menu): треба ОБИДВА — `tabIndex={open ? 0 : -1}` (керування клавіатурою) І `aria-hidden={!open}` на контейнері (accessibility tree / `getByRole`-запити в тестах). Сам лише `tabIndex` не прибирає елемент з a11y-дерева.
- **Будь-яке non-`none` значення `transform` на предку створює новий containing block для `position: fixed` нащадків** — реальна поведінка CSS, не браузерний квірк. `StaggerFadeIn`'s "shown"-стан колись резолвився в щось на кшталт `translateY(0)` (не `none`) — виглядає ідентично, але ламало будь-який `fixed inset-0` fullscreen-оверлей (VideoLightbox/ImageLightbox), відкритий з картки всередині нього: замість повного viewport оверлей рендерився відносно трансформованої картки. Фікс — mounted-стан резолвиться рівно в `"none"` (CSS transitions до/від `none` анімуються коректно, це identity-transform). Якщо додаєш новий `fixed`-оверлей — перевір, чи не сидить він усередині чогось із залишковим transform.
- **`prefers-reduced-motion`** — глобально в `globals.css`, одна media query скорочує всі animation/transition duration до near-zero. Нові анімації (marquee, drift, drum-обертання, typewriter) автоматично це поважають без додаткового коду — але власні `requestAnimationFrame`-цикли (drum, per-world backdrop-рухи) все одно мають explicit-перевіряти `matchMedia("(prefers-reduced-motion: reduce)")` і зупиняти нескінченний цикл (не просто зменшувати тривалість — RAF-цикл сам по собі не CSS-transition, глобальна media query його не чіпає).
- **Не комітити throwaway Playwright-скрипти верифікації** (`tests/_*.spec.ts`) — це усталена конвенція сесії: пишеш одноразовий скрипт для скріншот-перевірки чи `getBoundingClientRect()`-вимірювання, дивишся результат, видаляєш файл (і будь-який `test-results/`-вивід) перед завершенням.
- **CSS custom property не може починатись з цифри.** `--3d-signal-text` — невалідний ідентифікатор (CSS custom properties — `--` + `<ident>`, `<ident>` не може починатись з цифри без escape) і мовчки не парситься в деяких рушіях. Для 3D-світу токен називається `--world-3d-signal-text`, не `--3d-signal-text`.
- **`--signal` (і per-world `theme.signal`) — фіксований яскравий акцент, читається як glow/border/background wash в обох темах, але як ПЛОСКИЙ ТЕКСТ на світлій темі — ні.** Низько-альфа жовтий/помаранчевий/бірюзовий колір, накладений на майже-чорний фон (dark theme), читається як теплий підсвіт; той самий колір на майже-білому фоні (light theme) читається як пляма. Для тексту (не background/border/box-shadow) використовуй `text-signal-text`/`getWorldTheme(world).signalTextVar` — theme-aware CSS-змінна (`globals.css`, dark = `var(--signal)`, light = темніший відтінок), а не сам `--signal`/`theme.signal`. Той самий принцип стосується `textShadow`/glow-ефектів навколо великого декоративного тексту (`PageTitleWatermark`, `WorldTitleCube`'s ambient glow) — низько-альфа тінь, непомітна на темному фоні, може читатись як видима "смуга" на світлому; перевіряти скріншотом в обох темах, не лише в коді.
- **Компонент, що рендериться двічі за брейкпоінтом (dual-render, як-от `FlagIcon`/`HeroGridScrollBar`/тепер і `WorldSwitchHeaderNav`/`HeaderLogoButton`) — ставить ОБИДВІ копії в DOM одночасно**, просто одна `display:none` через `md:hidden`/`hidden md:flex`. Playwright-локатор на сирий атрибут (`page.locator('[aria-label="..."]')`) НЕ фільтрує по видимості і зловить strict-mode violation (2 елементи); `.first()` теж не рішення — бере перший у DOM-порядку, не перший видимий. Правильно — `getByRole(...)`/`getByLabel(...)`, які виключають `display:none` з accessibility tree.
- **Autoplay `<video>` на сторінці з багатьма прев'ю-кліпами (`/work/video`) — гейтувати через `useInView` (`src/hooks/useInView.ts`, IntersectionObserver), не рендерити з `autoPlay`/`preload="auto"` одразу на маунті.** До дюжини одночасних `autoPlay+loop+preload="auto"` відео (більшість за межами екрана) — реальна причина "підвисання" сторінки при завантаженні. `autoPlay`-атрибут спрацьовує лише раз при маунті і не реагує на подальші зміни видимості — керування play()/pause() робити імперативно через ref (той самий патерн, що й гучність/mute у VideoLightbox).
- **Дві окремі Tailwind-утиліти, кожна зі своїм `animation` shorthand, НЕ комбінуються на одному елементі.** CSS-каскад просто вибирає одного переможця для всього `animation`-property (за specificity/source order), а не мерджить список анімацій із двох class-правил. Реальний баг: `MetaversePortalBlock`'s блоби мали окремі класи `.animate-portal-blob-*` (transform-дрейф) і `.animate-portal-hue` (filter-переливання) на одному `<span>` — другий клас повністю перебивав перший, дрейф зникав. Фікс — один клас на блоб з comma-separated `animation: name1 …, name2 …;` в одному CSS-правилі; per-blob фазовий зсув — через `animation-delay: "0s, -7s"` (два значення, по одному на кожну назву в списку), не через окремі keyframes.
- **Контекстна навігація "назад" з кейс-стаді** — `GlassProjectBlock`'s лінк на `/work/[slug]` несе `?from=<world>` (project.world, завжди коректний, бо компонент рендериться лише всередині `WorldGallery`/`DevelopersFilter` для свого world). Case-study сторінка читає `searchParams.from`, звіряє з `worlds.includes(...)` (не довіряє довільному значенню з URL) і передає `backHref={`/work/${from}`}` у `BackToWorkButton`; немає/невалідний `from` → дефолтний `/work`. Той самий патерн — шаблон для будь-якої іншої "повернутись туди, звідки прийшов" навігації.
- **Агент, запущений з `isolation: "worktree"`, що сам ганяє `npm install`/`npm run build` всередині свого worktree — лишає nested `node_modules`/`.next` під `.claude/worktrees/agent-*`, які `npm run lint`'s broad glob підхоплює** (сотні хибних errors з generated Turbopack-коду). Швидка діагностика — `npx eslint "src/**/*.{ts,tsx}"` (скоуп лише на власний код). Якщо worktree-агент правив файл, який сесія вже встигла змінити (uncommitted) до створення worktree — `git apply`/`git diff`-мердж на контекстних лініях може не накластись (worktree форкнувся зі старішого commit, не з поточного working tree); надійніше — вручну перенести конкретні зміни агента в поточну версію файлу, звірившись з обома текстами. Прибирати worktree (`git worktree remove --force` + `git branch -D`) одразу після того, як усе потрібне з нього забрано.

## Команди
```bash
npm run dev      # Next.js dev server (Turbopack), http://localhost:3000
npm run build    # production build + strict TS typecheck
npm run lint     # ESLint
npm test         # Playwright suite (tests/*.spec.ts)
```

## Деплой
Продакшн: **https://serhiitarasiuk.space** (живий, задеплоєний з 2026-07-11).

- **Хостинг**: той самий Hetzner-сервер, що вже несе `FrameForg`/`PromoVideo` користувача, через Coolify (v4.1.2). Окремий Coolify-проєкт `portfolio` → environment `production` → один Application-ресурс, збирається з кореневого `Dockerfile` (Build Pack: **Dockerfile**, не Nixpacks — репо вже має власний, під нього написаний multi-stage Dockerfile з Next.js standalone-виводом, порт 3000).
- **Git**: GitHub `mrtarasyuks/portfolio`, гілка `main`. Coolify підключений напряму до цього репо (без окремого GitHub App — через простіший метод, деталі не критичні).
- **DNS**: домен куплений на Namecheap, нейм-сервери лишені стандартними (**Namecheap BasicDNS**, не Custom/Cloudflare). У Namecheap Advanced DNS — два A-записи: `@` і `www`, обидва на IP Hetzner-сервера (`95.217.159.18`). Стандартний "Redirect Domain" forwarding, який Namecheap ставить за замовчуванням при реєстрації, був видалений — він конфліктував з A-записами.
- **Workflow деплою** (усталений, повторюваний): зробити правки → `git commit` → `git push origin main` → користувач сам тисне **Redeploy** в Coolify UI (автодеплою по webhook немає, тож пуш сам по собі нічого не деплоїть). Claude комітить і пушить лише коли користувач явно про це просить (як завжди), але сам процес "коміт → пуш → користувач тисне Redeploy" — це вже нормальний, очікуваний фінальний крок кожної сесії правок, не разова дія.
- **Відео-хостинг** (`NEXT_PUBLIC_MEDIA_BASE_URL`) — налаштований і працює: окремий standalone Caddy-контейнер на тому ж Hetzner-сервері (не через основний Coolify-ресурс — `docs/media-hosting.md` спочатку припускав Coolify+Caddy-ресурс, але сервер реально ганяє Traefik, тож замість цього піднятий окремий контейнер з тими самими Traefik-лейблами, що й у portfolio-застосунку). `NEXT_PUBLIC_MEDIA_BASE_URL` виставлений у Coolify env vars, підтверджено користувачем "Все працює!". Локально (`npm run dev`) той самий відносний шлях резолвиться у `/public` як фолбек через `mediaUrl()`.

## Правила розробки
- Компоненти — функціональні, TypeScript strict, без `any`
- Кольори — Obsidian Signal token-система (не хардкодити hex), per-world акцент через `theme.signal` (`worldTheme.ts`); для light/dark chrome-теми — `--bg`/`--text`/`--surface`-і-т.д. CSS-змінні, не хардкодити hex
- Рядки — завжди через `copy.en.ts`/`copy.uk.ts` (типобезпечно звірені одне з одним через `copy.ts`), без хардкоду тексту
- Shape grammar: рівні глибокі кути для звичайних карток, `rounded-2xl` — **лише** для glass-поверхонь (`GlassPanel`) — свідомий виняток, не системний дрейф
- Нові діаграми кейс-стаді (`components/graphics/`) — кожна отримує власну "shape grammar" під свою тему (flow lines / matrix grid / circular loop / timeline), ніколи не переюзаний один компонент під п'ять різних сюжетів
- Нові одноразові Playwright-скрипти для візуальної перевірки чи pixel-вимірювання — не комітити, видаляти після використання
- Комміти — лише коли користувач явно попросить
