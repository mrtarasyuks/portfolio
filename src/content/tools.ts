export type VideoTool = {
  name: string;
};

/** Empty for now — real program names (and logos, later) land here as a one-object addition, same pattern as projects.ts, once supplied. */
export const videoTools: VideoTool[] = [];
